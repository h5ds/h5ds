/**
 * 设计JSSDK是为了更好的使用编辑器，而不依赖react环境
 * h5ds.init(params); // 初始化一个编辑器，一个页面，一个模块只可以初始化一个编辑器
 *
 * params:
 * {
 *   callback: () => {},
 *   before: () => {},
 *
 * }
 *
 * h5ds.getInitData(); // 机票钱初始化数据
 * h5ds.enums; //  eum 数据
 * h5ds.version; // 当前版本号
 *
 */
